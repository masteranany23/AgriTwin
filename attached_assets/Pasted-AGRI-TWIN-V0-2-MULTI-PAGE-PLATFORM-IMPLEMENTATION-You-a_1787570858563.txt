AGRI TWIN — V0.2 MULTI-PAGE PLATFORM IMPLEMENTATION

You are continuing development of the existing AgriTwin repository.

IMPORTANT:
This is NOT a redesign from scratch anymore.

The existing AgriTwin backend is the source of truth.

The frontend must become a real multi-page client for the existing backend APIs.

============================================================
1. CRITICAL BACKEND SAFETY RULE
============================================================

DO NOT modify the scientific backend implementation.

DO NOT modify:
- EnKF implementation
- WOFOST/PCSE simulation logic
- assimilation services
- observation repository logic
- scientific calculations
- scientific validation code
- existing backend tests
- database models unless absolutely required for a frontend/API compatibility bug

The backend has already undergone scientific audits and extensive testing.

Treat backend scientific code as FROZEN.

Frontend work should be isolated in the existing frontend directory.

If a backend API has an actual bug, DO NOT silently rewrite the backend.
Report the problem clearly.

============================================================
2. OBJECTIVE
============================================================

Transform the existing AgriTwin single-page demonstration UI into a
professional MULTI-PAGE agricultural digital twin platform.

The frontend must use the REAL AgriTwin backend APIs.

Do NOT continue using hard-coded demonstration values when a corresponding
backend API exists.

The existing Stitch/Replit visual design remains the visual language.

Keep the same:
- typography
- warm off-white background
- forest green
- muted agricultural tones
- restrained orange observation accent
- scientific minimalism
- subtle borders
- subtle animations
- generous whitespace

Do not turn this into a generic SaaS dashboard.

The product should feel like a scientific digital-twin research platform.

============================================================
3. FRONTEND TECHNOLOGY
============================================================

Use the existing frontend stack if already configured.

If necessary use:

- React
- TypeScript
- Vite
- React Router
- Recharts
- Framer Motion / Motion
- Tailwind CSS

Do not introduce unnecessary frameworks.

Create a centralized API client.

Use environment configuration:

VITE_API_BASE_URL

Example:

VITE_API_BASE_URL=http://localhost:8000

Never hard-code the backend URL throughout components.

============================================================
4. MULTI-PAGE ROUTING
============================================================

Create these routes:

/
    Overview

/fields
    Fields list

/fields/:fieldId
    Field Digital Twin

/simulations
    Simulation history

/simulations/:simulationId
    Simulation detail

/assimilation/:simulationId
    Assimilation dashboard

/observations
    Observation explorer

/satellite
    Satellite observation ingestion

/fusion
    Data fusion workspace

/scenarios
    Scenario analysis

/benchmark
    Scientific benchmark

/scout
    Field scouting

/about
    About AgriTwin / scientific methodology

============================================================
5. GLOBAL NAVIGATION
============================================================

Create a professional persistent navigation.

Desktop:

AgriTwin

Overview
Fields
Simulations
Assimilation
Observations
Forecast
Scenarios
Research

On smaller screens use a clean responsive navigation.

Show a small status indicator:

API CONNECTED
or
API OFFLINE

This should come from the /health endpoint.

Do not fake connection status.

============================================================
6. OVERVIEW PAGE
============================================================

Route:

/

Use the current approved Stitch design as the hero.

Keep:

A Digital Twin for Every Field.

Supporting description:

AgriTwin combines physics-based crop simulation, real-world observations
and sequential data assimilation to understand field conditions and
forecast what happens next.

Below the hero create a concise platform overview.

Show real backend-derived summary information where possible.

Include:

- number of fields
- number of simulations
- number of observations
- latest assimilation status

Do not invent values.

If the backend has no aggregate endpoint, derive these from existing list
endpoints without creating a new backend endpoint.

Keep the field visualization as the main visual element.

Add clear navigation cards/links to:

Field Digital Twin
Simulations
Assimilation
Observations
Scenarios

============================================================
7. FIELDS PAGE
============================================================

Route:

/fields

Use:

GET /fields

Support the backend's existing filters:

- farm_id
- latitude bounds
- longitude bounds
- limit
- offset

Display fields as clean scientific cards/table.

Each field should show:

- field name
- coordinates
- area
- elevation
- simulation count

Provide:

OPEN FIELD TWIN

which navigates to:

/fields/:fieldId

Provide:

CREATE FIELD

using:

POST /fields

Form fields:

- name
- latitude
- longitude
- area_ha
- elevation_m
- description
- optional farm_id
- optional boundary_geojson

For DELETE /fields/{field_id}:

Require explicit confirmation.

Clearly state that deletion is irreversible.

Do not hide destructive operations behind a normal button.

============================================================
8. FIELD DIGITAL TWIN PAGE
============================================================

Route:

/fields/:fieldId

This should be the main AgriTwin scientific visualization page.

Fetch:

GET /fields/{field_id}

Then show:

FIELD PROFILE

- field name
- coordinates
- area
- elevation
- simulation count

Provide a simulation selector if simulations exist.

Show the field visualization developed in v0.1.

Keep:

CURRENT STATE
ASSIMILATED
FORECAST

But now make the visualization respond to real backend data.

Show:

- crop state
- observation locations when available
- latest LAI
- soil moisture
- biomass
- development stage
- yield estimate

Do not invent values.

============================================================
9. SIMULATIONS PAGE
============================================================

Route:

/simulations

Use:

GET /simulations

Support filters:

- crop
- variety
- status
- run_type
- field_id
- limit
- offset

Show:

- crop
- variety
- simulation date
- status
- field
- run type

Each row/card must link to:

/simulations/:simulationId

Provide:

RUN NEW SIMULATION

using:

GET /simulate/crops
POST /simulate

The crop dropdown must be populated from:

GET /simulate/crops

Do not hard-code crop varieties.

Simulation form should support the actual backend request schema.

Read the backend Pydantic schema before implementing the form.

After submission:

- show progress/loading state
- display returned simulation_id
- navigate to simulation detail

============================================================
10. SIMULATION DETAIL
============================================================

Route:

/simulations/:simulationId

Use:

GET /simulations/{simulation_id}

Display:

SIMULATION INFORMATION

- crop
- variety
- location
- sowing date
- harvest date
- simulation metadata

Then display the real daily state time series.

Variables:

LAI
SM
TAGP
TWSO

Also display available variables from the response rather than assuming
future variables do not exist.

Create an interactive chart with variable selection.

Use scientific units.

Example:

LAI
m²/m²

SM
cm³/cm³

TAGP
kg/ha

TWSO
kg/ha

Do not silently change units.

============================================================
11. ASSIMILATION PAGE
============================================================

Route:

/assimilation/:simulationId

This page is the centerpiece of the scientific platform.

Use the real endpoints:

GET /assimilation/status/{simulation_id}

GET /assimilation/{simulation_id}/history

GET /assimilation/{simulation_id}/timeseries

GET /assimilation/{simulation_id}/yield-evolution

GET /assimilation/{simulation_id}/diagnostics

GET /assimilation/{simulation_id}/forecast

Also provide:

RUN ASSIMILATION

using:

POST /assimilation/run

Read the actual AssimilationRunRequest schema before creating the form.

Show assimilation state:

- status
- ensemble size
- total cycles
- executed cycles
- skipped cycles
- observations assimilated
- latest assimilation date

============================================================
12. ASSIMILATION HISTORY
============================================================

Use:

GET /assimilation/{simulation_id}/history

Each cycle contains:

- cycle date
- variables updated
- observation vector
- prior state
- posterior state
- innovation
- quality score
- cycle number

Create an interactive cycle explorer.

When a cycle is selected show:

PRIOR
OBSERVATION
INNOVATION
POSTERIOR

For example:

LAI:
Prior → Observation → Innovation → Posterior

Do not calculate these values again in JavaScript.

Display exactly what the backend returns.

============================================================
13. ASSIMILATION TIME SERIES
============================================================

Use:

GET /assimilation/{simulation_id}/timeseries

The backend provides:

LAI
SM
TAGP
TWSO
RFTRA

Each contains:

date
open_loop
assimilated
observation

Create variable tabs.

For each variable show:

OPEN LOOP
ASSIMILATED
OBSERVATION

Use a scientific chart.

The chart must be interactive:

- tooltip
- date
- values
- legend
- variable selection

============================================================
14. YIELD EVOLUTION
============================================================

Use:

GET /assimilation/{simulation_id}/yield-evolution

Show how predicted yield evolves across assimilation cycles.

Create:

Yield evolution chart

Show:

date
predicted yield kg/ha

Clearly distinguish:

current prediction

from

final forecast.

============================================================
15. ENKF DIAGNOSTICS
============================================================

Use:

GET /assimilation/{simulation_id}/diagnostics

and/or:

GET /benchmark/diagnostics/{simulation_id}

Create a scientific diagnostics page/panel.

Show the values returned by the backend for:

- innovation
- prior ensemble spread
- posterior ensemble spread
- valid observations
- rejected observations
- state update magnitude

Do not invent diagnostic metrics.

Use charts where appropriate.

This page should be useful for researchers and officials who want
evidence that assimilation is actually occurring.

============================================================
16. FORECAST PAGE / PANEL
============================================================

Use:

GET /assimilation/{simulation_id}/forecast

Display the real ForecastResponse.

Important:

The backend already provides:

- daily trajectories
- ensemble mean
- ensemble standard deviation
- 95% prediction intervals
- min/max
- yield forecast
- yield uncertainty
- open-loop yield
- assimilated yield
- optional hybrid yield
- observation summary
- forecast mode
- confidence explanation
- diagnostics

Do not reduce this to a single yield number.

Create a proper forecast visualization.

Trajectory selector:

LAI
SM
TAGP
TWSO
DVS
RFTRA

Show:

ensemble mean
95% prediction interval
optional min/max envelope

Also show:

OPEN-LOOP YIELD
ASSIMILATED YIELD
FORECAST YIELD
UNCERTAINTY

If hybrid_result is null, do not show fake hybrid data.

============================================================
17. OBSERVATIONS PAGE
============================================================

Route:

/observations

Use the real observation APIs.

Support:

POST /observations

POST /observations/register

POST /observations/batch

GET /observations

GET /observations/latest

GET /observations/by-variable

GET /observations/{id}

GET /observations/batches/{id}

Build an observation explorer.

Filters:

- field
- variable
- source
- status
- date range

Sources include:

SATELLITE
SENSOR
WEATHER
MANUAL
MODEL

Show:

- timestamp
- variable
- value
- units
- uncertainty
- source
- provider
- quality score
- cloud cover
- status

Create a time-series view for observations.

============================================================
18. SATELLITE PAGE
============================================================

Route:

/satellite

Use:

GET /satellite/lai

Allow user to select:

- field
- start date
- end date
- vegetation index
- maximum cloud cover
- uncertainty

Supported indices:

NDVI
OSAVI
SeLI

Display returned:

- acquisition date
- cloud cover
- NDVI
- OSAVI
- SeLI
- estimated LAI
- quality score
- metadata

Show a satellite LAI timeline.

Clearly label satellite values as observations.

Do not call them simulated values.

============================================================
19. DATA FUSION PAGE
============================================================

Route:

/fusion

Implement the actual backend data fusion APIs:

POST /fusion/fill-gaps

POST /fusion/spatial-align

POST /fusion/confidence

POST /fusion/fuse

POST /fusion/pipeline

Create a guided scientific workflow:

01 Temporal Gap Handling
02 Spatial Alignment
03 Confidence / R Estimation
04 Multi-source Fusion
05 Unified Observation Stream

The complete pipeline should call:

POST /fusion/pipeline

when the user chooses the full workflow.

For individual operations provide advanced controls.

Show returned quality flags and diagnostics.

For temporal interpolation clearly explain:

- interpolated observations
- cloud gaps
- HOLD_OPEN_LOOP conditions

Do not hide quality flags.

============================================================
20. SCENARIOS PAGE
============================================================

Route:

/scenarios

Implement the real scenario endpoints:

POST /scenarios/sowing-date

POST /scenarios/irrigation

POST /scenarios/variety

Create three scenario modes:

SOWING DATE
IRRIGATION
VARIETY

Each scenario should allow the user to choose a base simulation configuration.

For sowing date:

allow offset selection.

For irrigation:

use the actual backend request schema.

For variety:

populate crop/variety choices from:

GET /simulate/crops

Show scenario comparison results as charts.

The UI should make it clear these are model scenarios, not observations.

============================================================
21. BENCHMARK PAGE
============================================================

Route:

/benchmark

This is a scientific validation page.

Use:

POST /benchmark/evaluate

and:

GET /benchmark/diagnostics/{simulation_id}

Allow the user to select:

- simulation
- variable
- ground truth points

Read the actual BenchmarkEvaluateRequest schema.

Show the backend's returned comparison metrics.

Expected metrics may include:

MAE
RMSE
Bias
R²
confidence interval coverage

Only display fields actually returned by the backend.

The UI must clearly distinguish:

OPEN LOOP
ASSIMILATED
GROUND TRUTH

Do not manufacture ground truth.

This page should look like a scientific validation interface, not a generic
analytics dashboard.

============================================================
22. SCOUT PAGE
============================================================

Route:

/scout

Implement the currently available W-Shape scout endpoints.

POST:

/fields/{field_id}/scout-session

GET:

/fields/{field_id}/scout-sessions

GET:

/scout-sessions/{session_id}

The UI should allow exactly five image uploads.

Show the W-Shape concept visually.

However, IMPORTANT:

The current backend implementation explicitly indicates that the full
GPS EXIF / GRVI / LAI processing pipeline is still pending.

Therefore DO NOT pretend this is fully implemented.

Clearly label the feature:

EXPERIMENTAL / PIPELINE IN PROGRESS

If the backend returns a pending status, display that honestly.

Do not implement the scientific GRVI calculation in the frontend.

============================================================
23. ERROR CORRECTION
============================================================

DO NOT build the deprecated error-correction endpoint into the normal user
workflow.

The backend marks:

POST /error-correction/correct-window

as DEPRECATED.

The canonical architecture is:

observations
→ quality control
→ fusion
→ EnKF
→ assimilated WOFOST

Do not create a UI that encourages bypassing EnKF.

============================================================
24. API CLIENT ARCHITECTURE
============================================================

Create a centralized typed API layer.

Example:

src/
  api/
    client.ts
    fields.ts
    simulations.ts
    assimilation.ts
    observations.ts
    satellite.ts
    fusion.ts
    scenarios.ts
    benchmark.ts
    scout.ts

Do not put fetch() calls directly inside page components.

Create TypeScript types corresponding to backend Pydantic responses.

Important:

The frontend must consume the actual response shapes.

Do not guess field names.

Inspect the backend schemas before implementing each API adapter.

============================================================
25. DATA FLOW
============================================================

Use this architecture:

Backend API
    ↓
typed API client
    ↓
React query/data hooks
    ↓
page-level data
    ↓
reusable visualization components

Do not duplicate scientific calculations.

The frontend is responsible for:

- presentation
- interaction
- filtering
- charting
- navigation

The backend remains responsible for:

- WOFOST
- EnKF
- observations
- uncertainty
- data fusion
- forecasting
- benchmarking
- scientific calculations

============================================================
26. LOADING / ERROR / EMPTY STATES
============================================================

Every API-backed page must have:

Loading state
Error state
Empty state
Success state

Never leave a blank page when an API fails.

Show useful messages such as:

"Unable to load assimilation history."

"Backend API unavailable."

"No valid observations found for this field."

Do not expose raw Python stack traces to users.

Provide a retry button where appropriate.

============================================================
27. API OFFLINE MODE
============================================================

The application should NOT silently replace failed API calls with fake
demonstration data.

If the backend is unavailable:

show:

DEMO / API OFFLINE

only if a clearly defined demo mode is intentionally selected.

Otherwise show the actual API error state.

Never mix fake and real values on the same screen without clearly labeling
them.

============================================================
28. VISUALIZATION DESIGN
============================================================

Maintain the existing AgriTwin visual identity.

Use the current v0.1 Stitch design as the visual reference.

Do not turn every page into cards.

Use different layouts:

Field page:
large visualization + profile

Simulation page:
table + charts

Assimilation:
timeline + cycle inspector + charts

Observation:
data explorer + time series

Forecast:
large uncertainty charts

Scenario:
controls + comparison visualization

Benchmark:
scientific comparison

Keep the interface calm and professional.

============================================================
29. RESPONSIVE DESIGN
============================================================

Desktop:
optimized scientific workstation layout.

Tablet:
responsive two-column layouts.

Mobile:
single column.

Charts must resize correctly.

Tables should become horizontally scrollable inside their container rather
than causing page-level horizontal scrolling.

Navigation must remain usable on mobile.

============================================================
30. SECURITY / DESTRUCTIVE ACTIONS
============================================================

Never expose backend database IDs unnecessarily in the UI.

For DELETE operations:

- require confirmation
- explain consequence
- prevent accidental deletion

Do not store secrets in frontend code.

Use environment variables.

============================================================
31. PERFORMANCE
============================================================

Do not fetch large time series repeatedly.

Use appropriate caching/query invalidation.

Only request detailed simulation/assimilation data when the corresponding
page needs it.

Do not run expensive POST operations automatically when a page loads.

For:

POST /simulate
POST /assimilation/run
POST /fusion/pipeline
POST /scenarios/*

always require explicit user action.

Show an appropriate progress state.

============================================================
32. SCIENTIFIC TERMINOLOGY
============================================================

Use accurate terminology.

Do not simplify:

EnKF → "AI prediction"

WOFOST → "AI model"

Assimilation → "data update"

Observation → "sensor data"

Instead preserve scientific terminology and provide short explanations.

For example:

Ensemble Kalman Filter (EnKF)
Sequentially updates the simulated crop state using observations and
ensemble uncertainty.

============================================================
33. DEMO / OFFICIAL PRESENTATION MODE
============================================================

Keep the existing "DEMONSTRATION MODE" concept.

But make it honest.

If demonstration mode is active:

show a visible badge:

DEMONSTRATION MODE

If values are illustrative:

label them:

Illustrative demonstration data

If real backend data is being displayed:

label:

LIVE API DATA

Do not mix the two silently.

============================================================
34. HEALTH
============================================================

Use:

GET /health

to display backend connectivity.

Show:

API
Database
Version

when available.

============================================================
35. DO NOT ADD THESE FEATURES
============================================================

Do NOT add:

- authentication
- payments
- chatbot
- generic AI assistant
- weather dashboard unrelated to backend
- market prices
- disease detection
- farmer social network
- unrelated IoT dashboard
- fake satellite imagery
- fake real-time sensors
- fake predictions

Only implement functionality supported by the existing AgriTwin backend.

============================================================
36. IMPORTANT IMPLEMENTATION ORDER
============================================================

Implement in this order:

PHASE 1
Global navigation + React Router + API client

PHASE 2
Fields + Field Detail

PHASE 3
Simulations + Simulation Detail

PHASE 4
Assimilation + real visualization APIs

PHASE 5
Forecast

PHASE 6
Observations + Satellite

PHASE 7
Data Fusion

PHASE 8
Scenarios

PHASE 9
Benchmark

PHASE 10
Scout

Do not spend time redesigning the landing page.

============================================================
37. BACKEND/API DISCOVERY REQUIREMENT
============================================================

Before implementing each page, inspect the corresponding backend route
and Pydantic schema.

Do not infer request or response fields from the old demo data.

The backend API is the source of truth.

Relevant backend namespaces include:

/health

/simulate
/simulations
/fields
/fields/{field_id}/scout-session
/scenarios
/observations
/satellite
/assimilation
/fusion
/benchmark

============================================================
38. FINAL VERIFICATION
============================================================

After implementation:

1. Run frontend build.
2. Run TypeScript checks.
3. Run lint if configured.
4. Verify every route.
5. Verify API errors are handled.
6. Verify mobile layout.
7. Verify charts.
8. Verify no hard-coded scientific values remain where an API exists.
9. Verify the backend scientific files were not modified.
10. Verify existing backend tests remain untouched.

Create a short API integration report:

API ENDPOINT
→ FRONTEND PAGE
→ UI COMPONENT

For example:

GET /assimilation/{id}/timeseries
→ Assimilation page
→ TimeSeriesChart

GET /assimilation/{id}/diagnostics
→ Assimilation page
→ EnKFDiagnostics

GET /assimilation/{id}/forecast
→ Forecast page
→ ForecastTrajectory

Finally report:

- routes created
- API endpoints integrated
- endpoints intentionally not integrated
- backend files modified
- frontend files created
- build status
- remaining limitations